this application will have to exist for a while until I receive my fiancée's blessing to move to the redesign =]

this is the original template for the wedding website. components in the `@/components/legacy` directory only exist if the component in the `@/components/tenant` directory has diverged. this is the path forward; as compared to creating backwards compatible components. should only be used as a last resort.

"I see cinnamon"
